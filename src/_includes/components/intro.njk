<div class="intro">
  <h1 class="typing">{{ intro.data.name }}</h1>
  <p>{{ intro.data.designation }}</p>
</div>

<style>
.intro {
  text-align: center;
  margin-top: 20vh;
}
.typing {
  display: inline-block;
  overflow: hidden;
  white-space: nowrap;
  border-right: 2px solid $accent;
  width: 0;
  animation: typing 2s steps(30, end) forwards, 
             blink-caret .5s step-end infinite;
}
@keyframes blink-caret {
  50% { border-color: transparent; }
}
.intro p {
  margin-top: 1rem;
  font-size: 1.25rem;
  color: lighten($text, 20%);
}
</style>

